<h1>My Articles</h1>

<table class="table table-striped">
  <thead>
    <tr>
      <th>Name</th>
      <th>Link</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @bookmarks.each do |bookmark| %>
      <tr>
        <td><b><%= bookmark.name %></b></td>
        <td width="400px"><%= link_to "#{bookmark.link}", bookmark.link, target: "_blank" %></td>
        <td>
          <%= link_to 'More', bookmark, class: 'btn btn-primary' %>
          <%= link_to 'Edit', edit_bookmark_path(bookmark), class: 'btn btn-primary' %>
          <%= link_to 'Delete', bookmark, class: 'btn btn-primary', method: :delete, data: { confirm: 'Are you sure?' } %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Article', new_bookmark_path, class: "btn btn-success" %>
